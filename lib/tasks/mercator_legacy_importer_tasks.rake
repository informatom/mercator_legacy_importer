# encoding: utf-8

namespace :import do
  # starten als: 'bundle exec rake import:name
  # in Produktivumgebungen: 'bundle exec rake import:name RAILS_ENV=production'
  desc "Import name from legacy webshop"
  task :name => :environment do

  end
end